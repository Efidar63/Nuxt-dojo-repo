{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "set NODE_OPTIONS=--enable-source-maps && nuxi build",
    "dev": "set NODE_OPTIONS=--enable-source-maps && nuxi dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "node .output/server/index.mjs"
  },
  "dependencies": {
    "nuxt": "^3.15.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.13.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17"
  }
}
